# React Core API — 복습 퀴즈

> 생성일: 2026-02-12
> 소스: `docs/react/React-Core-API.md`

---

## [1/16] ReactElement & $$typeof 보안

**Q**: ReactElement의 `$$typeof`에 `Symbol.for()`를 사용하는 것이 XSS 방어에 효과적인 이유는 무엇인가요? 공격자가 DB에 `{ "$$typeof": "Symbol.for('react.transitional.element')" }`를 저장하는 시나리오를 기준으로 설명해주세요.

**A**: Symbol의 경우 직렬화/역직렬화가 불가능하여 공격자가 해당 예시처럼 공격하여도 코드 단위에서 Symbol이 아닌 String으로 판단되어 가드됨

**판정**: ✅ 통과

**보충**: JSON 스펙이 지원하는 6가지 타입(string, number, boolean, null, object, array)에 Symbol이 없으므로, `JSON.parse()` 후 `$$typeof`는 문자열이 되어 `isValidElement()`의 `=== REACT_ELEMENT_TYPE` 비교에서 실패한다.
> 📍 `packages/react/src/jsx/ReactJSXElement.js:892-898`

---

## [2/16] ref undefined→null 통일

**Q**: ReactElement 생성 시 `props.ref`가 `undefined`이면 `null`로 변환합니다. 왜 `undefined`를 그대로 두지 않고 `null`로 통일하나요?

**A**: 통일성 때문. 만약 undefined와 null 모두 허용하면 둘 다 체크하거나 모든 체크를 `!=`로 대체해야 함. 반면 null로 통일하면 `!== null`만 체크하면 됨.

**판정**: ✅ 통과

**보충**: Reconciler 전체에서 ref를 다루는 곳이 수십 군데인데, 입구에서 null로 통일하면 하류의 모든 코드가 `!== null` 하나로 안전해진다.
> 📍 `packages/react/src/jsx/ReactJSXElement.js:177-179`

---

## [3/16] jsx() vs createElement()

**Q**: jsx()와 createElement()의 핵심 차이 중 하나로, jsx()에서는 가능하지만 createElement()에서는 불가능한 최적화가 있습니다. 무엇이고, 왜 그런가요?

**A**: (힌트 사용) jsx()는 기계적으로, createElement()는 사람이 호출하는 차이점은 이해하고 있습니다. 다만 jsx()가 결국 매번 새 객체를 생성한다는 것을 보장한다는 것을 이해하였는데 이게 어떤 최적화로 이어지는지 잘 모르겠습니다.

**판정**: 🔶 아쉬움

**보충**: jsx()는 컴파일러가 항상 새 객체 리터럴을 전달하므로, React가 config 객체를 복사하지 않고 그대로 props로 재사용할 수 있다. createElement()는 같은 config 객체를 여러 번 넘길 수 있어서 반드시 새 객체로 복사해야 한다.
> 📍 `packages/react/src/jsx/ReactJSXElement.js:325-328`

**Q** (변형): createElement()에서 config 객체를 그대로 재사용하면 어떤 문제가 생길 수 있나요? 구체적인 코드 시나리오로 설명해주세요.

**A**:
```js
const sharedConfig = { name: "hello" };
createElement(MyComponent, sharedConfig);
sharedConfig.name = "world";               // config 수정!
createElement(MyComponent, sharedConfig);
```
근데 이게 왜 위험한 걸까요?

**판정**: ✅ 통과

**보충**: React가 첫 번째 createElement에서 sharedConfig를 복사하지 않고 그대로 props로 사용했다면, 이후 `sharedConfig.name = "world"` 시점에 이미 생성된 첫 번째 Element의 props까지 변경된다. Element는 불변이어야 하는데 외부에서 mutate되는 것.

---

## [4/16] Object.freeze — DEV 전용

**Q**: React는 Element의 props와 element 자체를 `Object.freeze()`하는데, 프로덕션에서는 하지 않습니다. DEV에서만 freeze하는 이유는 무엇인가요?

**A**: React 규칙 중 props는 불변(또는 조작 불가능 룰)을 프로그래밍적으로 가드하기 위함입니다. 이게 프로덕션에서 빠진 이유는 잘 모르겠습니다.

**판정**: 🔶 아쉬움

**보충**: `Object.freeze()`는 성능 비용이 있다. 매 Element마다 호출하면 V8의 hidden class 최적화에 영향. DEV에서 실수를 잡아주면 프로덕션에서는 freeze 없이도 안전하다고 판단.
> 📍 `packages/react/src/jsx/ReactJSXElement.js:276-278`

**Q** (변형): 프로덕션에서 freeze가 없을 때, 개발자가 실수로 `el.props.className = "b"`를 실행하면 어떻게 되나요?

**A**: `b`로 변경됩니다. 왜냐하면 별다른 가드가 없기 때문에.

**판정**: ✅ 통과

---

## [5/16] SharedInternals 슬롯 접근 방식

**Q**: SharedInternals의 5개 슬롯(H, A, T, S, G)은 접근 방식이 두 카테고리로 나뉩니다. 각 카테고리와 그 차이를 설명해주세요.

**A**: H, A는 메서드 참조라면, T, S, G는 값이 할당됩니다.

**판정**: 🔶 아쉬움

**보충**: H는 `resolveDispatcher()`라는 전용 리졸버 함수를 통해 접근하고, A는 `ReactSharedInternals.A`로 직접 접근한다. H에만 리졸버가 있는 이유는 모든 Hook이 H를 경유하므로 가장 빈번하게 호출되어 DEV 가드 + 인라이닝 최적화를 한 곳에 모으기 위함.
> 📍 `packages/react/src/ReactHooks.js:24-42`
> 📍 `packages/react/src/ReactHooks.js:44-51`

**Q** (변형): T 슬롯을 사용하는 `startTransition()`에서는 어떤 패턴으로 T 값을 다루나요?

**A**: save/restore 패턴

**판정**: ✅ 통과

**보충**: 이전 T 값을 저장 → 새 Transition 객체로 교체 → scope 실행 → finally에서 이전 값 복원.
> 📍 `packages/react/src/ReactStartTransition.js:45-117`

---

## [6/16] SharedInternals flatten 이유

**Q**: PR #28783에서 SharedInternals를 중첩 구조(`ReactCurrentDispatcher.current` 등)에서 플랫 구조(`H`, `A` 등)로 변경했습니다. 주된 이유는 무엇이었나요? (번들 크기가 아닙니다)

**A**: 구조 변경을 위한 리팩토링 (Dispatcher 전면 개편, async 컴포넌트 지원)

**판정**: ✅ 통과

**보충**: async 컴포넌트에서 `currentOwner`를 `AsyncLocalStorage`로 추적하기 위해 `CacheDispatcher`를 `AsyncDispatcher`로 확장하는 작업(PR #28912)의 사전 정리. 2순위로 서드파티 내부 의존성 차단, 번들 크기(-0.76kB)는 부차적 효과.

---

## [7/16] resolveDispatcher 매 호출 필요성

**Q**: `resolveDispatcher()`를 모듈 전역에 한 번만 호출해서 캐싱하면 안 되는 이유는 무엇인가요?

**A**: H의 값이 매번 재평가되어야 하기 때문(값이 변경되므로)

**판정**: ✅ 통과

**보충**: 모듈은 앱 시작 시 한 번만 평가되므로, 그 시점에 H는 null. 이후 렌더러가 H를 Mount/Update/ContextOnly Dispatcher로 교체해도 이미 캡처된 변수는 영원히 null.
> 📍 `packages/react/src/ReactHooks.js:38-41`

---

## [8/16] ContextOnlyDispatcher

**Q**: 렌더링이 완료된 후 H 슬롯에는 무엇이 설정되며, 그것의 역할은 무엇인가요?

**A**: ContextOnlyDispatcher, 의도한 에러 문구 출력을 위한 객체

**판정**: ✅ 통과

**보충**: `finishRenderingHooks()`에서 H를 `ContextOnlyDispatcher`로 교체하며, 거의 모든 Hook 메서드는 `throwInvalidHookError()`를 실행. null로 두면 의미 없는 에러가 나오지만, ContextOnlyDispatcher는 의미 있는 에러 메시지를 제공한다.
> 📍 `packages/react-reconciler/src/ReactFiberHooks.js:633-656`
> 📍 `packages/react-reconciler/src/ReactFiberHooks.js:3870-3896`

---

## [9/16] Server에서 빠지는 Hook

**Q**: 서버에서 `useState`, `useEffect`, `useRef` 등이 빠지는 이유를 "설계상 빠진 것"이 아니라고 학습했습니다. 그렇다면 실제 이유는 무엇인가요?

**A**: 여러 렌더에 걸쳐 효과를 보인다거나, 브라우저 페인트 등에 따라 동작한다든지 등의 서버에서는 불가능한 작업을 기반으로 하는 Hook들을 서버에서 제외

**판정**: ✅ 통과

**보충**: 서버 렌더링의 실행 모델(단일 패스, DOM 없음, 상태 없음)에서 물리적으로 동작 불가능한 것들. 서버에 남은 5개(use, useId, useCallback, useMemo, useDebugValue)는 전부 순수 계산이거나 부수효과가 없는 것들.

---

## [10/16] noopCache와 Shared Component

**Q**: 클라이언트에서 `cache()`는 캐싱을 하지 않는 noopCache입니다. 그런데 왜 아예 제거하지 않고 no-op으로라도 export하나요?

**A**: `cache()` 함수는 Shared Components에서 호출 가능. 또한 새로운 함수로 래핑하는 로직이 있음. 근데 no-op에서는 캐시가 안 되므로 그대로 반환하면 인자가 있는 함수라면 `fn.length`에서 다른 결과를 나타낼 수 있음.

**판정**: ✅ 통과

**보충**: Shared Component가 서버/클라이언트 양쪽에서 `import { cache } from 'react'`를 하므로 클라이언트에서도 export해야 빌드가 깨지지 않고, 래퍼 함수를 반환하여 `length === 0` 등 외부에서 관찰 가능한 특성을 서버의 `cache()`와 동일하게 맞춤.
> 📍 `packages/react/src/ReactCacheClient.js:21-23`

---

## [11/16] $$typeof 2단계 구조

**Q**: `$$typeof`는 2단계로 존재합니다. 각 단계가 어디에 위치하며, 각각의 목적은 무엇인가요?

**A**: element 레벨: React 엘리먼트인지 확인 → 실제로 아까 Symbol 문제처럼 외부 접근 차단 목적도 있음. type 레벨: memo, forwardRef 등 특수한 래핑 컴포넌트 분기용.

**판정**: ✅ 통과

**보충**: `element.$$typeof === REACT_ELEMENT_TYPE` (Element 레벨, 보안용), `element.type.$$typeof === REACT_MEMO_TYPE` (Type 레벨, Reconciler 분기용). 서로 다른 레벨이므로 충돌하지 않는다.

---

## [12/16] memo가 객체를 반환하는 이유

**Q**: `memo()`가 함수 래퍼 대신 객체를 반환해야 하는 이유는 무엇인가요? `createFiberFromTypeAndProps()`의 분기 구조와 연결지어 설명해주세요.

**A**: (힌트 사용) function 컴포넌트를 제일 먼저 체크함. 따라서 memo가 render 함수를 반환하면 해당 분기에 잡힘.

**판정**: ✅ 통과

**보충**: `typeof type === 'function'`이 첫 번째 분기이므로, memo가 함수를 반환하면 `FunctionComponent`로 분류되어 `$$typeof`를 확인하는 else 블록에 도달하지 못한다. 또한 Reconciler가 이전 props, compare 함수 등 메타데이터에 접근해야 하므로 객체가 필수.
> 📍 `packages/react-reconciler/src/ReactFiber.js:561-663`

---

## [13/16] createFiberFromTypeAndProps 분기 순서

**Q**: `createFiberFromTypeAndProps()`에서 `function → string → else` 순서로 분기하는 이유는 무엇인가요?

**A**: 함수 컴포넌트가 가장 일반적이고 많은 케이스를 다루기 때문에 대부분 1차 분기에서 걸려짐. 반대로 구성하면 대부분의 컴포넌트가 3단계 검증을 모두 거쳐야 할 수도 있음.

**판정**: ✅ 통과

**보충**: 빈도 기반 최적화. 앱에서 가장 흔한 함수 컴포넌트를 첫 번째 분기에 놓아 핫 패스에서 바로 끝나도록 한 의도적 설계.

---

## [14/16] lazy()의 default export 제약

**Q**: `lazy()`는 default export만 지원합니다. named export를 사용하려면 어떻게 해야 하나요?

**A**: `import(...).then(m => m.default)`

**판정**: 🔶 아쉬움

**보충**: lazy()가 항상 `moduleObject.default`를 반환하므로, named export를 사용하려면 `.then()`에서 named export를 `default` 키로 매핑해야 한다:
```js
const MyComponent = lazy(() =>
  import('./MyModule').then(mod => ({ default: mod.MyNamedComponent }))
);
```
> 📍 `packages/react/src/ReactLazy.js:203-216`

**Q** (변형): lazy()가 반환한 객체는 내부적으로 상태 머신을 갖고 있습니다. 몇 단계이며, 어떤 상태들인가요?

**A**: (패스) 모르겠음

**판정**: ❌ 오답

**보충**: lazy()는 4단계 상태 머신 — Uninitialized(-1): ctor 호출 전, Pending(0): ctor 호출 후 대기 중, Resolved(1): 성공, Rejected(2): 실패.
> 📍 `packages/react/src/ReactLazy.js:25-28`

**Q** (변형): lazy()의 상태 머신에서 `Uninitialized`과 `Pending`의 차이는 무엇인가요?

**A**: Uninitialized는 ctor 호출 전 / Pending는 ctor 호출 후 결과 대기

**판정**: ✅ 통과

---

## [15/16] cache() CacheNode Trie

**Q**: 서버의 `cache()`는 인자 조합을 어떤 자료구조로 캐싱하나요? 그리고 객체 인자와 원시값 인자에 따라 어떤 차이가 있나요?

**A**: 객체 인자는 WeakMap, 원시값은 Map / 객체 인자 GC 관리를 위해 WeakMap 사용

**판정**: ✅ 통과

**보충**: 인자 순서대로 가지를 타고 내려가는 Trie 구조이며, 객체 인자는 WeakMap으로 참조가 사라지면 캐시도 자동 삭제되고, 원시값은 GC 대상이 아니므로 Map을 사용.
> 📍 `packages/react/src/ReactCacheImpl.js:55-128`

---

## [16/16] startTransition T 슬롯 패턴

**Q**: `startTransition()`이 실행되는 동안 T, S 슬롯은 각각 어떤 역할을 하나요?

**A**: T가 실제로 작업하는 역할이라면 S는 매니저처럼 컨트롤하는 역할

**판정**: 🔶 아쉬움

**보충**: T 슬롯에는 Transition 객체가 설정되어 "이 안의 setState는 low-priority"라는 신호 역할. S 슬롯은 렌더러가 주입한 콜백으로, scope 실행 후 렌더러에 "여유 주문 접수 완료"를 통보하여 전체 스케줄을 조율.
> 📍 `packages/react/src/ReactStartTransition.js:45-117`

**Q** (변형): 레스토랑 비유에서 `startTransition`의 T 슬롯은 어떤 역할에 해당하나요?

**A**: 요리사?

**판정**: ❌ 오답

**보충**: T 슬롯 = 주문서에 붙이는 "여유" 태그. 보통의 setState는 급한 주문("테이블 3번 물이요!"), startTransition 안의 setState는 여유 주문("디저트요~ 바쁘면 나중에 해도 돼요"). 급한 주문이 들어오면 디저트 준비를 중단하고 급한 것 먼저(concurrent 인터럽트).
> 📍 `packages/react/src/ReactStartTransition.js:45-117`
