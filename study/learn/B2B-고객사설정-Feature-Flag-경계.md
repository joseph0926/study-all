

---

## 2026-03-10 (via /learn)

B2B 고객사 설정 테이블 vs feature flag 경계 학습. 호텔 체인 비유(객실세팅=Config, 시설키카드=Flag). 3단 판별 프레임워크(존재→변경주체→과금), Config/Flag/Limits 3영역 분리, DB 설계(tenant_configs + tenant_features + plan_limits), React Context 3분리 패턴. 당근비즈니스 실전 적용(비즈프로필/전문가모드/상품판매/쿼터).
