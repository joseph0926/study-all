interface:
  display_name: "Loop Code"
  short_description: "추상적 구현 요청을 설계, 계획, 검증 루프로 구체화합니다"
  default_prompt: "Use $loop-code to turn a vague implementation request into an agreed plan and verified code changes."
policy:
  allow_implicit_invocation: false
