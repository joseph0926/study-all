# 파서 정확성 & 테스트 실패 피드백

> 2026-02-23 (수정: 정밀 검증 반영) | 검토 대상: plan-parser.ts, session-parser.ts, meta-parser.ts, module-map.ts

> **주의**: Bash 실행 권한이 거부되어 `pnpm test` 결과는 정적 분석으로 대체. 실제 테스트 실행으로 검증 필요.

---

## 1. 테스트 실패 분석 (5건 — 정적 분석 기반)

### 실패 1: session-parser — completedSteps 수 불일치

**테스트**: `session-parser.test.ts:19` — "완료/미완료 스텝 분리"

```
expect(result.completedSteps.length).toBe(6);
expect(result.pendingSteps.length).toBe(0);
```

**실제 결과 (정적 분석)**: `completedSteps.length === 0`, `pendingSteps.length === 0`

**근본 원인**: fixture `test/fixtures/react/React-Core-API.md`에 2026-02-22 세션이 추가되었다. 파서는 마지막 세션(2026-02-22)을 사용하는데, 이 세션의 학습 로드맵 체크박스는 **모두 2칸 인덴트**되어 있다:

```markdown
- Step 1: ReactChildren — Children 순회 알고리즘
  - [x] 1.1: mapIntoArray 재귀 순회 — ...
  - [x] 1.2: Key 생성과 이스케이프 — ...
  - [ ] 1.3: Lazy/Thenable children 처리 — ...
```

파서(`session-parser.ts:80`)의 stepMatch 정규식:

```ts
const stepMatch = line.match(/^-\s+\[(x|X|\s)\]\s+(.+)$/);
```

`^-`는 줄 시작에 `-`를 요구한다. 인덴트된 `  - [x] 1.1:`은 `  -`로 시작하므로 **매칭되지 않는다**. 최상위 `- Step 1: ReactChildren`은 체크박스가 없으므로 역시 매칭 안 됨. 결과: completedSteps = 0, pendingSteps = 0, totalSteps = 0.

**테스트 기대값 `6`은 초기 fixture(2026-02-11 세션 기준, Step 1~6 전부 완료)의 잔재이다.**

**이중 문제**: (1) fixture/expected가 stale + (2) 파서가 인덴트된 체크박스를 처리하지 못함.

**해결 방향**:
- **파서 수정 (P2)**: 정규식에 선행 공백 허용 → `^\s*-\s+\[(x|X|\s)\]\s+(.+)$`
- **기대값 갱신 (P1)**: 파서 수정 후, 2026-02-22 세션 기준으로 completedSteps=2 (`1.1`, `1.2`), pendingSteps=9 (`1.3`~`5.2`)로 갱신

---

### 실패 2: session-parser — completedSteps 내용 불일치

**테스트**: `session-parser.test.ts:12` — "마지막 세션 재개점 추출" (부분)

```
expect(result.completedSteps).toContain("Step 1: ReactElement & $$typeof");
```

**실제 결과**: completedSteps가 빈 배열이므로 `.toContain()` 실패.

**근본 원인**: 실패 1과 동일. 마지막 세션(2026-02-22)의 인덴트된 체크박스가 파싱되지 않아 completedSteps = []. "Step 1: ReactElement & $$typeof"는 2026-02-11 세션의 스텝.

**해결 방향**: 파서 수정 후 기대값을 `"1.1: mapIntoArray 재귀 순회"` 등으로 변경.

---

### 실패 3: session-parser — 학습 요약 불일치

**테스트**: `session-parser.test.ts:27` — "학습 요약 추출"

```
expect(result.summary).toContain("React 패키지의 Core API Surface");
```

**실제 결과**: summary가 `` `React.Children.map()`의 핵심인 `mapIntoArray()`는... ``로 시작

**근본 원인**: 파서는 마지막 세션(2026-02-22)의 학습 요약을 추출하는데, 이 세션의 요약은 `React.Children.map()`에 대한 내용이다. 테스트 기대값 "React 패키지의 Core API Surface"는 2026-02-11 첫 세션의 요약.

**해결 방향**: 기대값을 2026-02-22 세션 요약의 일부(`React.Children.map()` 또는 `mapIntoArray`)로 변경.

---

### 실패 4: session-parser — 마지막 세션 날짜

**테스트**: `session-parser.test.ts:11` — "마지막 세션 재개점 추출"

```
expect(result.lastDate).toBe("2026-02-11");
```

**실제 결과**: `lastDate === "2026-02-22"` (expected '2026-02-22' to be '2026-02-11')

**근본 원인**: fixture에 2026-02-22 세션이 추가되어 마지막 세션 날짜가 바뀜.

**해결 방향**: 기대값을 `"2026-02-22"`로 변경.

---

### 실패 5: session.getResumePoint tool — 동일 원인

**테스트**: `test/tools/session.test.ts:19` — "session.getResumePoint returns existing resume"

```
expect(result.data.lastDate).toBe("2026-02-11");
```

**근본 원인**: 이 통합 테스트는 `docs/react/React-Core-API.md` (실제 docs 파일)를 읽는다. `STUDY_ROOT`가 `study-all/` 루트로 설정되어 있으므로 fixture가 아닌 실제 학습 기록 파일을 사용한다. 실제 파일에도 2026-02-22 세션이 추가되었으므로 동일한 불일치 발생.

**해결 방향**: 이 테스트도 fixtures를 사용하도록 변경하거나, 기대값을 현재 docs 파일 상태에 맞게 갱신.

---

## 2. 근본 원인 종합

### fixture/expected 동기화 메커니즘 부재

설계 문서(`plan/mcp.md` 섹션 7)에서 "기존 docs/ 파일을 fixtures로 복사하여 골든 테스트"를 명시하고 있다. 그러나:

1. **fixture 스냅샷 시점**: fixtures는 최초 생성 시점의 docs/ 상태를 반영
2. **docs/ 진화**: `/learn` 세션이 진행될 때마다 docs/ 파일에 새 세션이 추가됨
3. **동기화 트리거 없음**: docs/ 파일 변경 시 fixtures/expected를 자동 갱신하는 스크립트나 CI 체크가 없음

이 구조는 학습이 진행될수록 fixture가 stale해지는 것이 불가피하다.

**설계 갭**: 골든 테스트의 두 가지 전략 중 하나를 선택해야 한다:

| 전략 | 방법 | 장점 | 단점 |
|------|------|------|------|
| A. 고정 스냅샷 | fixture를 docs/와 독립적으로 유지. docs/ 변경 시 fixture는 건드리지 않음 | 안정적, 예측 가능 | docs/ 형식이 바뀌면 수동 갱신 필요 |
| B. 동기화 스냅샷 | `scripts/sync-fixtures.sh` 등으로 docs/ → fixtures 복사 + expected 재생성 | 항상 최신 | 복습/세션마다 fixture 갱신이 필요, 골든 값이 불안정 |

**권장**: **전략 A (고정 스냅샷)**. fixture는 한 번 만들면 건드리지 않는다. 테스트는 "파서가 이 고정 입력에서 올바른 출력을 내는가"만 검증한다. docs/ 파일의 형식이 바뀔 때(예: 새 섹션 추가)만 fixture를 의도적으로 갱신한다.

**즉시 조치**: 현재 fixture(`React-Core-API.md`)가 이미 2026-02-22 세션을 포함하고 있으므로, expected JSON과 테스트 하드코딩 값을 이 fixture의 현재 상태에 맞게 갱신해야 한다.

### tools/session.test.ts의 실제 데이터 의존

`session.test.ts`는 `STUDY_ROOT`를 `study-all/` 루트로 설정하여 `docs/react/React-Core-API.md` 실제 파일을 읽는다. 이 파일은 학습 진행에 따라 계속 변하므로 통합 테스트가 불안정하다.

**권장**: 통합 테스트도 fixtures를 기반으로 동작하도록 `STUDY_ROOT`를 fixtures 디렉토리로 변경하거나, tmpdir에 fixture를 복사하여 사용.

---

## 3. 파서별 정확성 심층 검토

### 3.1 plan-parser.ts — Coverage Analysis 파싱 엣지케이스

**파일**: `/Users/younghoonkim/dev/personal/@skills/study-all/mcp/src/parsers/plan-parser.ts`

#### 헤더 행 필터링 (line 36)

```ts
if (trimmed.includes("|--------")) {
  continue;
}
```

**문제**: 구분 행 판별이 `|--------` 서브스트링 포함 여부에 의존한다. 이 패턴은:
- `|---|---|---:|` 같은 정렬 표시(`:`)가 있는 구분 행도 매칭됨 (OK)
- `| --------description | ... |` 같은 데이터 행도 우연히 스킵될 수 있음 (이론적 위험이나 실제 plan.md에서는 발생 가능성 낮음)

**추가 누락**: 테이블 **헤더 행** 자체(| Status | Module | Skill Target |)가 필터링되지 않는다. `cols[0]`이 "Status"일 때 `mapStatus("Status")`가 `"uncovered"`를 반환하여 잘못된 CoverageRow가 생성된다.

실제로 `parseCoverageRows`의 결과에서 첫 번째 행이 헤더 데이터가 되지만, `parseCoverageSummary`(line 59-82)에서 `커버율**` 정규식 매칭이 우선되므로 coverage 집계에는 영향이 없다. 다만 `coverageRows` 배열 자체에 오염된 첫 행이 포함된다.

**권장**: 헤더 행 필터링을 추가하거나, 구분 행 이전의 모든 행을 건너뛰는 로직으로 변경:

```ts
// 현재
if (trimmed.includes("|--------")) { continue; }

// 권장: 구분 행 이후부터 데이터로 취급
let afterSeparator = false;
// ... 루프 내부
if (/^\|[\s-:|]+\|$/.test(trimmed)) {
  afterSeparator = true;
  continue;
}
if (!afterSeparator) continue;
```

#### 빈 셀 처리 (line 40-47)

```ts
const cols = trimmed.split("|").map((c) => c.trim()).filter(Boolean);
if (cols.length < 3) { continue; }
```

`filter(Boolean)`은 빈 문자열을 제거한다. `| ✅ | react | |` 같은 빈 Skill Target 셀에서 cols가 2개만 남아 행이 스킵될 수 있다. 현재 plan.md에서는 모든 행에 3열 이상이 채워져 있으므로 실제 문제는 없지만, 빈 Target 셀이 있는 plan.md가 생기면 해당 모듈이 누락된다.

**권장**: `filter(Boolean)` 대신 앞뒤 빈 문자열만 제거:

```ts
const cols = trimmed.split("|").map(c => c.trim());
// 첫/마지막 빈 문자열 제거 (| ... | 앞뒤)
if (cols[0] === "") cols.shift();
if (cols[cols.length - 1] === "") cols.pop();
```

#### Topic name 파싱 — 특수 문자 손실 (line 125-126)

```ts
const cleanTopicName = topicText.replace(/[✅⬜🔗].*$/, "").trim();
const module = cleanTopicName.split(/[\s—-]/)[0] || cleanTopicName;
```

이모지 이후의 모든 텍스트가 삭제된다. `### Topic 1: react/src/hooks ✅ 커버 — 23 files`에서 `cleanTopicName`은 `"react/src/hooks"`가 되어 정상이다. 하지만 `module` 추출에서 `-` 문자로 split하므로, `react-reconciler`가 `react`로 잘린다.

```ts
"react-reconciler".split(/[\s—-]/)[0]  // → "react"
```

**영향**: `topicDocsMapping`의 키가 `"react"`가 되어 다른 Topic과 충돌할 수 있다.

**권장**: module 추출 로직을 개선하거나, 전체 cleanTopicName을 module로 사용:

```ts
// 옵션 A: split 구분자에서 - 제거 (하이픈은 모듈명의 일부)
const module = cleanTopicName.split(/[\s—]/)[0] || cleanTopicName;

// 옵션 B: slug 함수 적용
const module = slug(cleanTopicName);
```

---

### 3.2 session-parser.ts — 다중 세션 경계 견고성

**파일**: `/Users/younghoonkim/dev/personal/@skills/study-all/mcp/src/parsers/session-parser.ts`

#### 세션 경계 정규식 (line 13)

```ts
const match = line.match(/^##\s+(\d{4}-\d{2}-\d{2})(?:\s*\(.+\))?$/);
```

이 정규식은 `## 2026-02-11 (재개 — Step 5부터)` 형식을 지원한다. fixture에서 실제 사용된 형식:

- `## 2026-02-11` -- 매칭
- `## 2026-02-11 (재개 — Step 5부터)` -- 매칭
- `## 2026-02-11 (재개 — Step 6부터)` -- 매칭

**견고성 우려**: 날짜 뒤에 괄호 없이 다른 텍스트가 오면 매칭 실패:

```markdown
## 2026-02-22 — Children 순회
```

`$` 앵커와 `(?:\s*\(.+\))?` 때문에 이 형식은 매칭되지 않는다. 현재 CLAUDE.md의 세션 기록 구조 규칙에서 날짜 헤더 형식을 `## YYYY-MM-DD` 또는 `## YYYY-MM-DD (설명)` 으로 제한하고 있으므로 당장 문제는 없으나, 형식이 확장될 경우를 대비하면:

```ts
// 더 유연한 버전
const match = line.match(/^##\s+(\d{4}-\d{2}-\d{2})\b/);
```

다만 이렇게 하면 `## 2026-02-11 이후의 날짜 형식 오류` 같은 비-세션 헤더도 매칭될 수 있어 트레이드오프가 있다. 현재 엄격한 정규식이 더 안전하다.

#### `---` 구분선 처리

설계 문서에서 `## YYYY-MM-DD`로 경계를 잡는다고 명시하고 있으며, 파서도 이를 따른다. 하지만 `session.appendLog`(`session.ts:117`)에서 새 세션 추가 시 `---` 구분선을 삽입한다:

```ts
const payload = `\n\n---\n\n## ${today} (${marker})\n\n${parsed.content.trim()}\n`;
```

`---`는 마크다운에서 수평선으로 렌더링되지만, 파서는 이를 무시한다 (세션 경계는 `## YYYY-MM-DD`로만 판별). 이는 올바른 설계이다. `---`는 시각적 구분 역할만 하고, 파서의 경계 로직에는 영향을 주지 않는다.

그러나 기존 fixture 파일의 첫 `---` (line 5, `## 병합 정리 기준` 이전)와 중간 `---` (세션 간 구분)가 혼재한다. `---`가 세션 구분자로 오인될 가능성은 없지만, 문서 규약 상 `---`의 역할을 명확히 해두는 것이 좋다.

#### 학습 로드맵 내 중첩 스텝 (2026-02-22 세션)

2026-02-22 세션의 학습 로드맵 구조가 이전 세션과 다르다:

```markdown
- Step 1: ReactChildren — Children 순회 알고리즘
  - [x] 1.1: mapIntoArray 재귀 순회 — ...
  - [x] 1.2: Key 생성과 이스케이프 — ...
  - [ ] 1.3: Lazy/Thenable children 처리 — ...
- Step 2: ReactContext — Context 생성 구조
  - [ ] 2.1: createContext 내부 — ...
```

파서의 체크박스 정규식 `^-\s+\[(x|X|\s)\]\s+(.+)$`는 **줄 시작이 `- [`** 인 행만 매칭한다. 인덴트된 `  - [x] 1.1:`은 줄 시작에 공백이 있으므로:

- `^-\s+` 는 줄 시작에 `-`를 요구 → 인덴트된 행의 경우 `  -`로 시작하여 `^-`와 매칭 안 됨

즉, 인덴트된 서브스텝의 체크박스가 파싱되지 않는다. 이것이 실패 1의 직접적 원인이다.

fixture의 2026-02-22 세션에서:
- 최상위 `- Step N:` 행은 체크박스가 없으므로 스킵 (올바름)
- 인덴트된 `  - [x] 1.1:` 행은 `^-`에 매칭 안 됨 (문제)
- 결과: completedSteps = 0, pendingSteps = 0, totalSteps = 0

fixture 파일(line 675-689)의 들여쓰기는 **2칸 공백**이다:

```markdown
- Step 1: ReactChildren — Children 순회 알고리즘
  - [x] 1.1: mapIntoArray 재귀 순회 — ...
```

`^-`가 아니라 `  -`로 시작하므로 정규식 실패. 파서가 이 세션에서 어떤 스텝도 인식하지 못한다.

**권장**: 파서의 체크박스 정규식을 인덴트 허용으로 변경:

```ts
// 현재
const stepMatch = line.match(/^-\s+\[(x|X|\s)\]\s+(.+)$/);

// 권장: 선행 공백 허용
const stepMatch = line.match(/^\s*-\s+\[(x|X|\s)\]\s+(.+)$/);
```

---

### 3.3 meta-parser.ts — 테이블 vs YAML-like 형식

**파일**: `/Users/younghoonkim/dev/personal/@skills/study-all/mcp/src/parsers/meta-parser.ts`

#### 두 가지 파싱 모드

1. **테이블 모드** (line 32-51): `| Concept | Level | ... |` 형식
2. **YAML-like 모드** (line 53-66): `- concept: ..., level: ..., streak: ...` 형식

YAML-like는 테이블 모드에서 0건일 때만 시도되는 폴백이다.

#### Canonical 형식 미정의

설계 문서에서 `-meta.md`의 형식을 명시적으로 정의하지 않았다. 테스트(`meta-parser.test.ts`)는 테이블 형식만 사용한다. YAML-like 형식의 테스트가 없다.

**권장**:
1. canonical 형식을 테이블로 확정하고, `review.saveMeta`도 테이블 형식으로만 쓰도록 강제
2. YAML-like 폴백은 레거시 호환으로 유지하되, 별도 테스트 케이스 추가:

```ts
it("YAML-like 폴백 파싱", () => {
  const fixture = `- concept: A, level: L2, streak: 1, next: 2026-03-01, graduated: false`;
  const meta = parseMeta(fixture, "Topic");
  expect(meta.concepts.length).toBe(1);
  expect(meta.concepts[0].name).toBe("A");
});
```

#### 헤더 행 필터링 (line 39)

```ts
if (cols.length < 5 || cols[0].toLowerCase() === "concept") {
  continue;
}
```

헤더 행을 `cols[0].toLowerCase() === "concept"`로 판별한다. 만약 개념 이름이 "Concept"이면 해당 행이 스킵된다. 실제로 이런 이름이 올 가능성은 거의 없지만, 더 견고한 방법:

```ts
// 구분 행(|---|)을 기준으로 헤더/데이터 분리 (plan-parser와 동일한 패턴)
```

#### attempts 필드 누락 가능

```ts
concepts.push({
  name: cols[0],
  level: normalizeLevel(cols[1]),
  streak: Number(cols[2]) || 0,
  nextReview: cols[3],
  graduated: parseBoolean(cols[4]),
  attempts: Number(cols[5]) || 0,  // cols[5] 없으면 0
});
```

5열 테이블(Attempts 열 없음)에서도 동작하지만, `cols.length < 5` 체크와 `cols[5]` 접근 사이에 `cols.length >= 6` 체크가 없다. 5열 테이블에서 `cols[5]`는 `undefined`이고, `Number(undefined)`는 `NaN`이므로 `NaN || 0 → 0`으로 폴백된다. 동작은 올바르지만 의도를 명시하면 좋겠다.

---

### 3.4 module-map.ts — maxDepth=2와 깊은 프로젝트

**파일**: `/Users/younghoonkim/dev/personal/@skills/study-all/mcp/src/parsers/module-map.ts`

#### walkFiles maxDepth=2의 영향

`buildModuleMap` (line 115):
```ts
const files = await walkFiles(dir, 2, 0);
```

각 모듈 디렉토리 내에서 최대 2단계 깊이까지만 파일을 탐색한다.

**React 소스 기준 분석**: `packages/react/`의 구조:

```
packages/react/
├── index.js              (depth 0)
├── src/                  (depth 1)
│   ├── ReactHooks.js     (depth 1)
│   ├── jsx/              (depth 2)
│   │   └── ReactJSXElement.js  (depth 2)  ← 포함
│   └── forks/            (depth 2)
│       └── ...           (depth 2)  ← 포함
```

depth 2이면 `src/*/`까지 커버된다. 대부분의 React 패키지는 `src/` 바로 아래에 파일이 있으므로 충분하다.

**하지만 깊은 구조의 프로젝트에서는 부족할 수 있다**:

```
packages/react-reconciler/
└── src/
    └── __tests__/
        └── utils/
            └── helpers.ts  (depth 3) ← 누락
```

이 경우 `fileCount`가 실제보다 적게 보고된다. 테스트/유틸리티 파일 누락은 `fileCount` 정확도에만 영향을 미치고, 모듈 식별 자체에는 영향이 없다(모듈은 1단계 디렉토리로 식별되므로).

#### 위험 수준 평가

| 시나리오 | maxDepth=2로 충분? | 영향 |
|----------|-------------------|------|
| React 패키지 (src/ 바로 아래) | 충분 | - |
| Next.js (src/server/lib/...) | 부족할 수 있음 | fileCount 부정확 |
| Monorepo (packages/pkg/src/core/utils/) | 부족 | fileCount 부정확 |
| 단일 앱 (src/features/auth/hooks/) | 부족 | fileCount 부정확 |

**권장**: `maxDepth`를 설정 가능하게 하거나, 기본값을 3~4로 올리는 것을 고려:

```ts
export async function buildModuleMap(sourceDir: string, opts?: { maxDepth?: number }): Promise<ModuleMapResult> {
  const maxDepth = opts?.maxDepth ?? 3;
  // ...
  const files = await walkFiles(dir, maxDepth, 0);
}
```

#### entryPoints 탐지의 한계

```ts
function detectEntryPoints(moduleName: string, files: string[]): string[] {
  // ...
  return files.map(file => path.basename(file)).filter(base => candidates.has(base));
}
```

`files`는 상대 경로 배열인데, `path.basename(file)`을 사용한다. 만약 서로 다른 하위 디렉토리에 동일 basename 파일이 있으면 중복이 발생할 수 있다:

```
packages/react/index.ts        → basename: "index.ts" ← 실제 엔트리포인트
packages/react/src/index.ts    → basename: "index.ts" ← 내부 파일
```

두 번 `"index.ts"`가 반환된다. `entryPoints`는 정보 제공 용도이므로 치명적이지는 않지만, 루트의 엔트리포인트만 필요하다면 depth 0 파일로 제한하는 것이 정확하다.

#### DEFAULT_IGNORE의 `.study` 포함

```ts
const DEFAULT_IGNORE = new Set([
  // ...
  ".study",
]);
```

`.study`는 프로젝트 모드에서 학습 기록 디렉토리인데, module-map은 소스 코드를 스캔하므로 올바르게 제외하고 있다. 다만 `docs/`는 제외되지 않는다. 만약 `sourceDir`이 study-all 루트를 가리킨다면 `docs/`도 모듈로 인식될 수 있다. 실제로는 `sourceDir`이 `ref/` 하위를 가리키므로 문제없다.

---

## 4. 즉시 조치 항목 (우선순위순)

### P1: 테스트 기대값 갱신 (5개 실패 해결)

> **주의**: P2(인덴트 허용)를 먼저 적용한 후 기대값을 결정해야 한다. 파서 수정 전후로 completedSteps 값이 달라지기 때문.

| 파일 | 변경 사항 |
|------|----------|
| `test/parsers/session-parser.test.ts:11` | `lastDate` → `"2026-02-22"` |
| `test/parsers/session-parser.test.ts:12` | `completedSteps.toContain(...)` → P2 수정 후 `"1.1: mapIntoArray..."` 등으로 변경 |
| `test/parsers/session-parser.test.ts:19` | `completedSteps.length` → P2 수정 후 `2`, `pendingSteps.length` → `9` |
| `test/parsers/session-parser.test.ts:27` | `summary` 기대값 → `"React.Children.map()"` 또는 `"mapIntoArray"` 포함으로 변경 |
| `test/expected/react-session-resume.json` | 전체 갱신 (현재 테스트에서 import하지 않으나 문서 목적으로 동기화 권장) |
| `test/tools/session.test.ts:19` | fixtures 기반으로 전환하거나 기대값 갱신 |

**참고**: `test/expected/*.json` 파일들은 현재 테스트 코드에서 직접 import/사용되지 않는다. 테스트는 하드코딩된 기대값을 사용하고 있다. expected JSON은 문서/참조 목적으로만 존재하므로, 테스트 코드에서 import하도록 변경하거나, 현재 상태를 유지하되 JSON도 동기화하는 방침을 정해야 한다.

### P2: session-parser 인덴트 허용

체크박스 정규식에 선행 공백 허용:

```ts
// session-parser.ts:80
const stepMatch = line.match(/^\s*-\s+\[(x|X|\s)\]\s+(.+)$/);
```

이것은 2026-02-22 세션의 중첩 스텝을 올바르게 파싱하기 위해 필요하다.

### P3: plan-parser 헤더 행 필터링

Coverage Analysis 테이블에서 헤더 행이 CoverageRow에 포함되지 않도록 필터링 추가.

### P4: plan-parser module 추출 하이픈 보존

`cleanTopicName.split(/[\s—-]/)` 에서 `-`를 제거하여 `react-reconciler` 같은 하이픈 모듈명이 잘리지 않도록 수정.

---

## 5. 장기 개선 권장

| 항목 | 설명 |
|------|------|
| fixture 관리 전략 확정 | 고정 스냅샷(A) vs 동기화 스냅샷(B) 중 선택하고 문서화 |
| YAML-like meta 테스트 추가 | 폴백 경로의 커버리지 확보 |
| module-map maxDepth 설정화 | 프로젝트별로 깊이를 조정 가능하게 |
| tools/session.test.ts 격리 | 실제 docs/ 파일 의존 제거, fixtures 기반으로 전환 |
| 파서 공통: 구분 행 판별 통일 | `|---|` 패턴을 정규식으로 엄격하게 매칭 (`/^\|[\s-:|]+\|$/`) |
